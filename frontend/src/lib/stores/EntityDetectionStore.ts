import { writable } from "svelte/store";

export const isEntityDetectionStore = writable(false);
export const isEntittDetectionLoadingStore = writable(false);
export const showEntityLabelStore = writable(false);
